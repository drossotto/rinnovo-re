services:
  - type: web
    name: rinnovo-registrar
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn python.registrar.app:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11

